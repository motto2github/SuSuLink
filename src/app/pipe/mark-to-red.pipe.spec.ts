import { MarkToRedPipe } from './mark-to-red.pipe';

describe('MarkToRedPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkToRedPipe();
    expect(pipe).toBeTruthy();
  });
});
